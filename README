
 SYNOPSIS
   Dotfiles Manager deploys a Git repo to
   $HOME and provides Git shell aliases

 USAGE
   SHOW THIS
     $ curl -Ls v.gd/dotman | sh

   DEPLOY A REPO
     $ curl -Ls v.gd/dotman | sh -s <repo>

   ALIASES
     DOWNLOAD
       $ curl -Ls v.gd/dotman | sh -s a

     HELP
       $ gx

